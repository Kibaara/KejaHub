module.export ={
    apps: [
        {
            name: "project-management",
            script: "npm",
            env: {
                MODE_ENV: "development"
            }
        
        }

    ]
}